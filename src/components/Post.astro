---
const { title, slug, category, price, weight } = Astro.props.post;
---

<article>
  <a href={`/post/${slug}`}>
    <p>{category}</p>
    <h2>{title}</h2>
    <p>R${price}</p>
    <p>{weight}g</p>
  </a>
</article>
